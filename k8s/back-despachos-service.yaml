apiVersion: v1
kind: Service
metadata:
  name: back-despachos
  namespace: proyecto-semestral
spec:
  type: ClusterIP
  selector:
    app: back-despachos
  ports:
    - port: 8081
      targetPort: 8081